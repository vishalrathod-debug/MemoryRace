services:
  - type: web
    name: memoryrace-game-server
    runtime: node
    rootDir: memory-game-server
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
